import 'package:flutter/material.dart';

class ReserveSummaryPage extends StatelessWidget {
  const ReserveSummaryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}